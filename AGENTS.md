# eChat AI 协作上下文

这份文档主要给参与本项目的 AI 读取，也给项目作者回看决策背景。任何 AI 在做产品、设计、前端、后端、部署、算法或大模型相关改动前，都应先理解这里的项目定位和交付边界。

## 项目背景

eChat 当前是一个个人独立开发练手项目，核心目标不是快速堆一个一次性 demo，而是探索“AI 深度参与个人软件开发”的完整过程。

这个项目会被用来练习和验证：

- 产品构思和需求拆解。
- UI/UX 设计和响应式网页实现。
- 前端工程、状态管理、实时交互和音视频体验。
- 后端服务、WebSocket 信令、部署链路和运行诊断。
- WebRTC、实时通信、大模型、算法或其他可探索技术栈。
- AI 如何通过文档、注释、设计稿和代码结构持续介入项目。

因此，项目可以带有探索性质，但不能停留在“随手写的 demo”。生成的东西应尽量达到“稍微调整即可上线给真实用户试用”的水平。

## 产品定位

当前产品方向是一个面向真实网页使用场景的语音房应用。项目定位为“技术探索型轻量完整产品级”：它首先要像一个可以被真实用户理解和试用的小产品，页面数量、功能入口和交互流程应保持克制；同时它也是用于探索 AI 参与独立开发、前后端工程、实时通信、算法、大模型和其他主流/前沿技术栈的实践项目。

- 页面要能被普通用户理解，不只给开发者或 AI 看。
- 核心页面优先控制在 2 到 4 个主页面，后续扩展也尽量保持在 3 到 6 个页面以内。
- 设置、邀请、帮助、权限说明等低频内容优先使用弹窗、抽屉或折叠区，不急着拆成大量独立页面。
- 操作流程要自然，不能只为了证明技术能跑。
- 文案要使用用户语言，避免直接暴露 WebRTC、ICE、SDP、信令、ping/pong 等技术词。
- 功能可以克制，但已出现的功能要尽量完整、清楚、可使用。
- 允许保留探索入口，但不要把页面做成杂乱实验场。

一句话判断：这是一个练手项目，但应该达到技术探索型轻量完整产品级，而不是粗糙样例。

## 设计原则

设计要兼顾年轻、现代、可上线和可实现。

- 避免企业后台味太重，也避免花哨到影响使用。
- 页面不需要很多复杂入口，但已有入口要有明确作用。
- 用户可见文案要自然，例如“连接成功”“声音流畅”“正在重新连接”，不要写“WebRTC stable”或“信令心跳正常”。
- 桌面端和手机端可以共用视觉语言，但布局应响应式适配。
- 设计稿要说明每个区域的作用、按钮含义、状态来源和后续实现方式。
- UI 不应只追求截图好看，还要考虑真实交互、空状态、错误状态、加载状态和离开/重连等边界。

如果 AI 生成设计图或设计稿，需要同步留下说明文档，说明：

- 每个板块为什么存在。
- 每个按钮做什么。
- 哪些内容是用户可见文案。
- 哪些内容只是开发映射或调试信息。
- 桌面端和手机端如何适配。

## 创意与产品感

eChat 希望最终可以上线给真实用户试用，因此 AI 在做产品方案、设计图、前端页面、交互流程或功能规划时，不应只满足“功能能跑”，也要适度思考产品是否有记忆点、情绪价值和年轻用户愿意尝试的理由。

创意不是堆功能，也不是盲目追热点。更合适的方向是：

- 给核心体验增加一点轻量但有辨识度的表达，例如房间氛围、进入仪式、邀请文案、状态反馈、声音相关的小互动。
- 关注当下用户熟悉的社交、AI、轻量陪伴、即时互动、分享传播等趋势，但只把趋势作为灵感输入，不直接照搬热点。
- 优先选择低成本、容易实现、不会破坏主流程的小创意。
- 创意应服务于“打开网页后自然地和朋友说话”这一核心体验。
- 如果创意会增加页面复杂度、打断用户进入房间、引入明显维护成本，应先作为备选方案说明，而不是直接塞进实现。
- 面向真实用户的创意要自然，不要显得像技术展示、AI 炫技或概念 demo。
- 可以在方案中额外给出“基础实现”和“创意增强”两层，让项目作者决定是否采用。

AI 在输出设计或产品方案时，可以主动补充：

- 一个稳妥可上线版本。
- 一个更有记忆点的轻量创意版本。
- 这些创意会影响哪些页面、交互、文案或实现成本。

## 创意触发时机

当任务涉及产品方案、设计图、前端页面、用户交互、用户文案、分享传播、新功能规划或上线准备时，AI 应主动思考是否可以加入轻量创意，让产品更有记忆点和真实用户使用感。

强触发场景包括：

- 生成产品方案、页面方案、设计图或交互方案。
- 实现首页、声聊间、邀请弹窗、离开页等用户直接看到的页面。
- 编写用户可见文案、按钮文案、状态文案、空状态和错误提示。
- 规划新功能、AI 能力、多人互动、房间氛围、分享传播能力。
- 项目准备上线、对外展示或给真实用户试用。

轻触发场景包括：

- 优化前端组件、页面布局、状态展示或弹窗体验。
- 修复影响用户体验的 bug。
- 编写产品文档、设计规范或后续 AI 接手文档。
- 发现当前页面或功能显得过于 demo、普通、冷冰冰或缺少产品感。

触发创意思考时，AI 不应直接把所有创意塞进实现。更合适的输出方式是：

- 先给出稳定可上线的基础方案。
- 再给出 1 到 3 个轻量创意增强。
- 说明每个创意影响哪些页面、交互、文案或实现成本。
- 除非用户明确选择，否则创意增强默认作为建议，不主动扩大实现范围。

## 技术与实现原则

项目允许探索新技术，但每次引入都要服务于当前目标。本项目不追求永远使用最简单实现；只要目的明确、边界清楚、代码可解释、文档能接住，允许为技术探索引入受控复杂度。

- 优先沿用项目已有技术栈和目录结构。
- 可以尝试当前主流或有一定前瞻性的新技术，但要说明为什么引入、解决什么问题、后续如何维护。
- 不为了炫技引入庞大框架或复杂架构；复杂度必须服务于学习、验证、扩展或可维护性。
- 代码定位是“轻量可维护”：不要写成一次性 demo，也不要过早拆出企业级分层和大量抽象。
- 优先用清晰模块、明确类型、必要注释和可验证流程支撑迭代。
- 对核心用户路径要认真实现，对探索性能力要模块化收纳，避免污染主流程。
- 允许为了探索引入更完整的状态模型、更规范的协议类型、更可观测的日志、更清晰的模块边界或更前瞻的工程结构。
- 可以逐步演进，但每一步最好都能运行、能验证、能解释。
- 前端状态、信令消息、后端数据结构和部署配置要保持可追踪。
- 实时通信相关改动要注意连接生命周期、资源释放、重连、心跳、日志和错误提示。
- 面向用户的状态和面向调试的状态要分开。

当前技术栈大致包括：

- 前端：React、TypeScript、Vite、Nginx。
- 后端：Go、WebSocket 信令服务。
- 实时通信：WebRTC 音频通信。
- 部署：Docker、Docker Compose、GitHub Actions、Nginx 网关。

## AI 介入方式

AI 不只是写代码，也需要帮助维护项目上下文。

当 AI 做较大改动时，应尽量留下能让下一个 AI 接手的材料：

- 必要的代码注释。
- 新增或更新的设计说明。
- 关键技术选择的原因。
- 重要调试方式和日志含义。
- 前后端协议、状态字段、消息类型的解释。
- 运行、构建、部署或验证方式。

AI 生成的结果应该让项目作者也能理解，而不是只能由 AI 自己继续维护。

## 注释与文档要求

本项目的注释不是越多越好，而是要帮助人和 AI 理解业务含义、数据流和边界条件。

- 前端代码应多解释状态、事件、组件职责、用户交互和数据流。
- 后端代码应简洁说明函数职责、参数含义、返回值、并发/连接生命周期和错误处理。
- TypeScript interface、type、union 字面量、配置对象字段要说明字段含义。
- WebSocket / WebRTC / 部署配置等容易混淆的地方，要保留必要解释。
- 避免只复述语法的注释。
- 如果用户明确说“加注释”，字段和枚举值也应补上注释。

文档要服务于两类读者：

- 项目作者：需要知道为什么这样做、后续怎么改。
- 后续 AI：需要快速掌握背景、边界和当前设计意图。

## 项目级规则修改权限

`AGENTS.md` 是项目级 AI 协作规则，影响后续所有 AI 的判断和行为。除非用户明确要求修改本文件，否则 AI 不应主动改动 `AGENTS.md`。

如果 AI 认为需要调整 `AGENTS.md`，应先说明：

- 准备新增、修改或删除什么规则。
- 为什么该规则需要进入项目级文档。
- 这会影响后续哪些类型的任务。

只有在用户明确同意后，才能修改 `AGENTS.md`。

## 交付标准

AI 完成任务时，不应只交一个能跑的片段。根据任务大小，尽量满足：

- 功能或设计符合真实使用场景。
- 用户界面不显得廉价或临时。
- 文案不暴露不必要技术细节。
- 代码结构便于继续扩展。
- 必要注释和文档已同步。
- 能构建或能通过相关测试时，应运行验证。
- 如果没法验证，要明确说明没验证什么。

## 复杂度边界

项目需要“五脏俱全”，但不追求一开始就企业级。它允许受控复杂度，但复杂度要有明确探索目的。

应该避免：

- 过早拆出大量抽象层。
- 页面入口过多，用户不知道先做什么。
- 为了完整性堆权限、后台、报表、复杂运营功能。
- 把探索性功能直接暴露给普通用户。
- 为了快速展示而写难以维护的一次性代码。
- 把“技术探索”理解成随意堆新框架、新概念和新页面。

更合适的做法是：

- 先做核心用户路径。
- 让页面达到基本可上线观感。
- 把复杂能力收进清晰模块。
- 通过文档记录探索方向和后续计划。
- 每次扩展都能解释它对产品或技术探索的价值。
- 让复杂度有理由、有边界、有注释、有验证方式。

## 当前语音房方向提示

当前语音房正在从两人通话探索到多人语音房。

设计和实现时需要注意：

- 多人房不应继续用“本地音频 / 远程音频”这种二人视角组织 UI。
- UI 应围绕成员、席位、说话状态、静音状态、房主、邀请和连接恢复组织。
- 桌面端可以使用“多人席位网格 + 右侧成员/动态栏 + 底部控制栏”。
- 手机端更适合“多列席位网格 + 底部抽屉 + 固定控制栏”。
- 用户看到的是“语音已连上、声音流畅、正在重新连接”；开发内部再映射到 WebRTC、信令、ICE、心跳等状态。

相关设计与产品文档优先参考：

- `designs/core-pages.md`：核心页面产品逻辑、按钮作用、状态含义和页面范围。
- `designs/core-pages-design.html`：首页、声聊间、离开页、设置弹窗、邀请弹窗的视觉设计稿。
- `designs/design-guidelines.md`：设计稿生成和前端实现时的文案、可读性、状态展示规则。
- `designs/ai-development-blueprint.html`：AI 参与产品、设计、前后端开发的协作背景和驾驶舱设计。

## 给后续 AI 的工作提醒

在开始实现前，先问自己：

- 这次改动是产品、设计、前端、后端、部署还是算法探索？
- 它是否符合“技术探索型轻量完整产品级，而不是粗糙 demo”的定位？
- 用户是否能理解页面上的文字和操作？
- 项目作者是否能通过注释和文档理解这次改动？
- 下一个 AI 是否能通过现有文档继续推进？
- 有没有把技术细节错误地展示给普通用户？
- 如果任务涉及前端页面、交互、视觉或用户文案，是否已经先阅读 `designs/core-pages.md` 和 `designs/design-guidelines.md`，再参考 `designs/core-pages-design.html` 实现？

如果答案不清楚，先补清楚上下文，再动代码。
