# Agent gRPC 服务依赖
# 安装方式：pip install -r agent/requirements.txt

# OpenAI 兼容客户端（调用 DeepSeek API）
openai>=1.0.0

# HTTP 调试接口
aiohttp>=3.9,<4

# gRPC
grpcio>=1.70.0
protobuf>=4.25.0

# YAML 配置文件解析
pyyaml>=6.0

# 结构化日志
loguru>=0.7.0

# JSON Schema 参数校验（工具系统）
jsonschema>=4.0.0

# 配置加载：类型化配置模型，替代手写 YAML 解析
pydantic>=2.0
