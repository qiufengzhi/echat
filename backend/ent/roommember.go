// Code generated by ent, DO NOT EDIT.

package ent

import (
	"echat-backend/ent/room"
	"echat-backend/ent/roommember"
	"echat-backend/ent/user"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// RoomMember is the model entity for the RoomMember schema.
type RoomMember struct {
	config `json:"-"`
	// ID of the ent.
	// 主键，写入时由服务端生成
	ID uuid.UUID `json:"id,omitempty"`
	// 所属房间 id，外键 rooms.id
	RoomID uuid.UUID `json:"room_id,omitempty"`
	// 在场用户 id，外键 users.id
	UserID uuid.UUID `json:"user_id,omitempty"`
	// 加入时的昵称快照，用于归档展示
	Username string `json:"username,omitempty"`
	// 最近一次加入时间
	JoinedAt time.Time `json:"joined_at,omitempty"`
	// 离开时间，可空；为 null 表示当前在场
	LeftAt *time.Time `json:"left_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RoomMemberQuery when eager-loading is set.
	Edges        RoomMemberEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RoomMemberEdges holds the relations/edges for other nodes in the graph.
type RoomMemberEdges struct {
	// 所属房间
	Room *Room `json:"room,omitempty"`
	// 在场用户
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RoomOrErr returns the Room value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RoomMemberEdges) RoomOrErr() (*Room, error) {
	if e.Room != nil {
		return e.Room, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: room.Label}
	}
	return nil, &NotLoadedError{edge: "room"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RoomMemberEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RoomMember) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case roommember.FieldUsername:
			values[i] = new(sql.NullString)
		case roommember.FieldJoinedAt, roommember.FieldLeftAt:
			values[i] = new(sql.NullTime)
		case roommember.FieldID, roommember.FieldRoomID, roommember.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RoomMember fields.
func (_m *RoomMember) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case roommember.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case roommember.FieldRoomID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field room_id", values[i])
			} else if value != nil {
				_m.RoomID = *value
			}
		case roommember.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		case roommember.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				_m.Username = value.String
			}
		case roommember.FieldJoinedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field joined_at", values[i])
			} else if value.Valid {
				_m.JoinedAt = value.Time
			}
		case roommember.FieldLeftAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field left_at", values[i])
			} else if value.Valid {
				_m.LeftAt = new(time.Time)
				*_m.LeftAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RoomMember.
// This includes values selected through modifiers, order, etc.
func (_m *RoomMember) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRoom queries the "room" edge of the RoomMember entity.
func (_m *RoomMember) QueryRoom() *RoomQuery {
	return NewRoomMemberClient(_m.config).QueryRoom(_m)
}

// QueryUser queries the "user" edge of the RoomMember entity.
func (_m *RoomMember) QueryUser() *UserQuery {
	return NewRoomMemberClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this RoomMember.
// Note that you need to call RoomMember.Unwrap() before calling this method if this RoomMember
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RoomMember) Update() *RoomMemberUpdateOne {
	return NewRoomMemberClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RoomMember entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RoomMember) Unwrap() *RoomMember {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RoomMember is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RoomMember) String() string {
	var builder strings.Builder
	builder.WriteString("RoomMember(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("room_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RoomID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("username=")
	builder.WriteString(_m.Username)
	builder.WriteString(", ")
	builder.WriteString("joined_at=")
	builder.WriteString(_m.JoinedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.LeftAt; v != nil {
		builder.WriteString("left_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// RoomMembers is a parsable slice of RoomMember.
type RoomMembers []*RoomMember
